# Tool stack

For semantic search across all notes, use the Memvid MCP server (see [[memvid]]).
It is the primary retrieval path; grep is the fallback.

For cross-project code search use [[recall]]; it is used daily and replaced manual grep.

Task tracking lives in [[tasks-db]].
